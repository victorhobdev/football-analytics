# Ingestion

Historical data ingestion utilities used to enrich competitions, squads, matches, and long-range football records.

Source modules live under `src/`. Operational orchestration remains in `infra/` and `platform/`.
